# put in root folder
[tox]
envlist = py{310, 311, 312, 313}

[testenv]
deps =
    pytest
    coverage
commands =
    coverage run -m pytest --junit-xml=coverage/{envname}/report.xml -s tests
    coverage xml -o coverage/{envname}/coverage.xml

